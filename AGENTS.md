项目名称

Windows Rogue Software Cleaner

项目目标

这是一个 Windows 桌面程序，用于帮助用户扫描、识别、选择并清理电脑中的流氓软件和异常组件。

主要清理对象包括但不限于：

- 壁纸插件
- 桌面弹窗广告
- 开机自启动广告程序
- 浏览器劫持插件
- 捆绑安装软件
- 异常常驻进程
- 不必要的杀毒软件
- 无明确来源的后台服务
- 计划任务中的广告程序
- 注册表中的异常启动项

本项目的核心原则是：

先扫描识别，再让用户确认，最后执行清理。禁止未经用户确认直接删除任何内容。

---

产品流程

完整流程必须遵循：

1. 扫描系统
2. 识别可疑项目
3. 展示清理列表
4. 用户选择要清理的项目
5. 执行清理
6. 清理后验证
7. 生成结果报告

---

核心功能

1. 扫描识别

程序需要扫描以下位置：

- Windows 启动项
- 注册表启动项
- 计划任务
- 系统服务
- 当前运行进程
- 已安装程序列表
- 浏览器扩展目录
- 桌面快捷方式
- AppData 目录
- Program Files 目录
- Program Files (x86) 目录
- Temp 临时目录

重点识别：

- 开机弹广告程序
- 常驻后台进程
- 无签名程序
- 来源不明程序
- 名称伪装成系统组件的程序
- 高频弹窗程序
- 浏览器主页劫持
- 壁纸插件
- 捆绑软件
- 安装后难以卸载的软件

---

安全规则

禁止行为

AI 编程助手不得实现以下行为：

1. 不得绕过 Windows 权限控制
2. 不得隐藏程序自身进程
3. 不得关闭系统安全机制
4. 不得静默删除文件
5. 不得在没有用户确认的情况下清理项目
6. 不得删除 Windows 系统核心文件
7. 不得破坏 Windows Defender
8. 不得清理用户个人文件
9. 不得上传用户隐私数据
10. 不得执行不可逆操作，除非用户明确确认

---

清理规则

清理操作必须尽量可恢复。

优先级如下：

1. 停止进程
2. 禁用启动项
3. 禁用计划任务
4. 禁用服务
5. 移动文件到隔离区
6. 删除残留文件
7. 清理注册表残留

删除前必须记录：

- 文件路径
- 注册表路径
- 服务名称
- 计划任务名称
- 原始状态
- 操作时间
- 操作结果

---

隔离区规则

程序必须提供隔离区功能。

隔离区用于保存被清理前的文件或配置备份。

隔离区至少记录：

- 原始路径
- 隔离路径
- 文件哈希
- 文件大小
- 清理时间
- 风险等级
- 还原方式

用户可以从隔离区还原被清理项目。

---

风险分级

扫描结果必须分级展示。

高风险

满足以下特征之一：

- 明确恶意广告程序
- 无法正常卸载
- 开机自动弹窗
- 浏览器主页劫持
- 伪装系统进程
- 隐藏在异常目录中
- 无数字签名且行为异常

中风险

满足以下特征之一：

- 不必要的自启动程序
- 未知厂商软件
- 捆绑安装软件
- 常驻后台但用途不明确
- 频繁修改系统设置

低风险

满足以下特征之一：

- 正常软件的可选组件
- 用户可能需要的软件
- 可疑但证据不足
- 仅占用资源但无明显恶意行为

---

用户确认规则

任何清理操作前，必须让用户看到：

- 项目名称
- 文件路径
- 厂商信息
- 风险等级
- 风险原因
- 建议操作
- 是否可恢复

用户必须主动选择清理项目。

默认不得全选高风险项目，除非用户明确点击全选。

---

验证规则

清理完成后，必须验证：

- 目标进程是否已停止
- 启动项是否已移除或禁用
- 服务是否已禁用
- 计划任务是否已删除或禁用
- 文件是否已删除或隔离
- 注册表项是否已清理
- 弹窗相关进程是否未重新启动

验证结果必须展示给用户。

---

报告规则

清理完成后生成报告。

报告包括：

- 扫描时间
- 扫描项目数量
- 发现风险数量
- 用户选择清理数量
- 成功清理数量
- 清理失败数量
- 失败原因
- 隔离文件数量
- 建议重启项
- 后续建议

---

权限规则

程序应尽量以普通权限运行。

只有在必要时才请求管理员权限。

请求管理员权限前必须说明原因，例如：

- 需要清理系统级启动项
- 需要禁用系统服务
- 需要修改注册表
- 需要删除 Program Files 中的软件残留

---

技术建议

优先使用以下技术路线：

- 语言：C# 或 Rust
- 桌面框架：WPF、WinUI 3、Tauri 或 Avalonia
- 系统接口：Windows API
- 数据存储：SQLite
- 日志系统：本地日志文件
- 配置文件：JSON 或 TOML

如无明确要求，优先选择 C# + WPF，因为它更适合快速开发 Windows 桌面工具。

---

代码规范

1. 扫描逻辑、清理逻辑、UI逻辑必须分离
2. 所有危险操作必须有日志
3. 所有删除操作必须优先进入隔离区
4. 所有模块必须有错误处理
5. 不得使用硬编码绝对路径
6. 不得写死中文路径
7. 不得阻塞 UI 主线程
8. 扫描和清理任务必须支持取消
9. 所有外部命令调用必须校验参数
10. 所有路径处理必须防止误删父目录

---

推荐模块结构

/src
  /Core
    Scanner
    Cleaner
    Verifier
    RiskEngine
    Quarantine
    Report
  /Windows
    RegistryReader
    StartupReader
    ServiceReader
    TaskSchedulerReader
    ProcessReader
  /UI
    Pages
    Components
    ViewModels
  /Data
    Models
    Database
  /Utils
    Logger
    Hash
    PathSafe

---

AI 编程助手工作规则

当用户提出开发需求时，AI 编程助手必须：

1. 先阅读现有项目结构
2. 判断需求影响哪些模块
3. 不修改无关文件
4. 优先实现最小可用版本
5. 保留安全边界
6. 不实现绕过系统安全机制的功能
7. 不实现静默清理功能
8. 不实现隐蔽运行功能
9. 每次修改后说明改动内容
10. 每次修改后列出需要测试的功能

---

输出格式

每次完成开发任务后，必须输出：

## 完成内容

## 修改文件

## 风险点

## 测试建议

## 下一步建议

---

项目底线

本项目是安全清理工具，不是攻击工具。

所有功能必须以用户知情、用户确认、可恢复、可审计为前提。

任何可能导致误删、破坏系统、绕过权限、隐藏行为、窃取数据的实现都必须拒绝。